host build

*	cd ./arch/host/nativecode; make clean; make

*	[More on host build here].	


i386 build

	The i386 build requires NASM 0.97 or higher; a distribution is
available at [URL].  To boot the i386 build, you'll need either GRUB
(available at [URL]) or Etherboot 4.0 (available at [URL]).  'make grub'
or 'make etherboot' as appropriate.  Instructions on using/installing GRUB
are avaivable at [URL, or local copy].
